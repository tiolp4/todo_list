#!/usr/bin/env sh
# Placeholder gradlew script. This is a minimal stub and may need the gradle wrapper jar to actually run.
echo "Gradle wrapper placeholder. To run the project build, add a proper gradle wrapper or install Gradle."
